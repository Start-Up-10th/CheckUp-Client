# 기술 결정과 확인 과제

사용자는 기술 세부사항을 백엔드·AI·프론트·데브옵스에 위임했다. 이 목록은 추가 제품 인터뷰 목록이 아니다.
`기본값`은 이 하네스에서 택한 구현 해석, `검증 필요`는 외부 증거가 필요한 사항이다. 사용자 확정과 혼동하지 않는다.
담당자는 증거·날짜·결과를 붙여 갱신하고 제품 의미가 달라질 때만 사용자에게 차이를 제시한다.

| ID | 상태·담당 | 결정 / 확인할 내용 |
| --- | --- | --- |
| DEC-001 | 기본값 · 백엔드/웹 | DataGSM 관리자는 관리자 홈으로, 학생만 동의→얼굴 등록 흐름으로 이동. 학생과 관리자 역할은 서버에서 판정. |
| DEC-002 | 검증 필요 · 백엔드 | userinfo가 반환하는 studentId 의미/타입·role·전체 명단 API·호실 변동·전학/퇴사/졸업 신호 확인. 제공된 필드 외 상태 필드를 발명하지 않음. 전체 목록을 가져오지 못하면 200명 통계가 완성됐다고 보고하지 않음. |
| DEC-003 | 검증 필요 · AI | MediaPipe 검출/랜드마크와 신원 임베딩을 분리. 임베딩 모델/라이선스·차원·거리·threshold·20개 대표 선택 방법을 실측. 원본 미보관·재등록 없음 때문에 모델 업그레이드 시 벡터 호환성 확보. |
| DEC-004 | 기본값 · AI/웹 | `3회 초과`를 4번째 연속 실패부터 QR 안내로 해석. 영상 프레임 수가 아닌 트랙별 시도 단위; 성공/이탈 때 초기화. 원문의 `임계값 3회`와 혼재되어 있음을 기록. |
| DEC-005 | 검증 필요 · AI/웹/백엔드 | 브라우저 오프라인 인식이 되려면 로컬 모델 실행도 필요. 벡터 캐시만으로 안 됨. 기기별 캐시 접근·삭제·인증·시계 오차·원본 비저장 검증. 불가능하면 정책을 몰래 온라인 전용으로 바꾸지 말고 해당 기능의 한계를 보고. |
| DEC-006 | 기본값 · 백엔드 | 운영일은 Asia/Seoul 08:00~다음 날 07:59:59.999. local time에서 8시간 뺀 날짜를 키로 사용. 중복 키는 student+purpose+운영일. 자정 초기화하지 않음. |
| DEC-007 | 기본값 · 백엔드/웹 | QR 화면 이탈은 best-effort 종료 통지 + 서버 lease/heartbeat로 처리. TTL/갱신 빈도는 담당자 결정. 갱신 실패해도 토큰 원래 만료는 유지. 용도 변경 시 기존 세션 종료. |
| DEC-008 | 기본값 · 백엔드 | 수동 수정의 현재 revision과 자동 이벤트 발생 시각을 구분. 수동 수정 전 이벤트의 늦은 도착은 무시하고 이후 새로운 인증은 반영. 당일 순서 정보는 감사 이력 UI가 아니며 08시 폐기. |
| DEC-009 | 기본값 · 백엔드/데브옵스 | 출석 완료 알림은 당일 출석과 같은 수명. 그 외 알림·공지·봉사 보관기간, 학생 설정의 범위, 백업 복원 제외/삭제 방법은 담당자가 운영 기록으로 확정. |
| DEC-010 | 기본값 · 백엔드/웹 | 봉사 명단 제외는 누적 횟수를 보존(첨부 문안 채택). 봉사 활동 등록 알림은 +1 적립에 연결하고 단순 명단 추가와 구분. 횟수 가감 UI는 DEC-014에서 사용자 확정(웹 구현과 Figma 근거는 DEC-023). |
| DEC-011 | 위임 · 데브옵스 | VM 크기·실제 PROJECT_OWNER 권한·포트/TLS·레지스트리·배포 트리거·롤백·백업 위치/기간·암호화 키. 미정 값은 빈 설정으로 두고 운영 적용 전에 검증. |
| DEC-012 | 위임 · 웹 | 공지 관리/조회 진입, 휴대폰 관리자 로그아웃(→DEC-014/026으로 확정), 상태 노출 시간, 이름 동일 시 안정 정렬. 핵심 UI/권한은 명세 유지. |
| DEC-013 | 확정 · 하네스/데브옵스 · 2026-09-23 | CI/CD 구성과 정적 검사는 서비스 개발 전에 준비 가능. 실행 가능한 골격·명령부터 테스트/빌드를 연결하며 전체 기능 완성을 기다리지 않는다. 미구현·비활성·건너뛴 작업은 검증 성공으로 간주하지 않는다. 실제 배포는 산출물·대상 환경과 요청 범위가 갖춰진 경우 실행한다. 근거: SRC-HARNESS-CI. |
| DEC-014 | 확정 · 제품/웹 · 2026-09-24 | SRC-NOTION-CHECKUPZIP 중 얼굴 등록 자동 카운트다운·촬영중·완료 흐름과 다시 찍기/완료 조작, 관리자 휴대폰 홈/QR/얼굴/봉사/로그아웃 5탭, 관리자 봉사 횟수 즉시 +1/-1·0 미만 방지를 채택. ZIP의 다른 항목은 이 결정으로 승인되지 않는다. 학생은 누적 횟수만 조회한다. |
| DEC-015 | 기본값 · 웹 | `web/` 골격: Next.js 16.3.6(App Router, Turbopack) + React 19.3.0 + TypeScript 6.0.3 + Tailwind CSS 3.4.19. TypeScript 7.0.2는 최신이지만 `typescript-eslint`(eslint-config-next 16 의존)가 아직 지원하지 않아(2026-09-23 확인) 6.0.3으로 고정. ESLint는 9.39.5로 고정(10.x는 `eslint-config-next`를 flat-compat으로 감쌀 때 `eslint-plugin-react`의 순환 참조로 깨짐). 새 프로젝트 스캐폴딩 시점의 `npm audit` 0건 기준으로 선정했으며, 추후 재검증 없이 방치하지 않는다. |
| DEC-016 | 기본값 · 웹 | 본문 폰트는 Pretendard(사용자 제공 `@font-face`, jsdelivr CDN)를 쓴다. Figma 스타일 가이드(343:95)에 "Pretendard · 프로젝트 기본 폰트 · Figma 미설치 상태"로 명시돼 있고 화면상 Noto Sans KR 표기는 그 대체 표시였다. 숫자 강조(호실 번호·시간)는 기존대로 Roboto Mono 유지. 모달 폭은 스타일 가이드(523:28)의 컴퓨터 460px을 채택했다 — 처음 확인했을 때 호실 상세 다이얼로그 인스턴스(540:1003)는 480px로 그려져 있어 20px 차이가 있었으나, 이후 디자이너가 인스턴스를 460px로 고쳐 둘이 일치한다(2026-09-23 재확인). |
| DEC-017 | 기본값 · 웹 | QR 코드 렌더링은 `qrcode.react`(4.2.0, MIT)를 쓴다. 실제 서명 토큰 발급은 서버 담당이므로(REQ-ATT-004) 프론트는 서버가 줄 문자열을 QR로 그리기만 하면 된다. 외부 QR 이미지 생성 API는 쓰지 않는다(토큰이 제3자 서버로 나가는 것을 피함). QR 코드 생성 화면(`AdminQrGeneration`)은 마운트 이후에만 mock 세션을 만든다 — Math.random 기반 값을 최초 렌더에서 바로 쓰면 SSR과 첫 클라이언트 렌더 결과가 달라져 hydration mismatch가 나기 때문이며, 이 로딩 구간은 REQ-UI-006 스켈레톤으로 채운다. |
| DEC-018 | 기본값 · 웹 | 얼굴 인식 생성 화면은 카메라 하드웨어 접근(`getUserMedia`)은 실제로 연결하되, 인식 결과(신원 대조)는 AI/Spring 연동 전까지 고정 mock으로 둔다(`mock-recent-recognitions.ts`) — 얼굴 검출·랜드마크를 신원 확인과 동일시하지 않는다는 원칙에 따라 브라우저에서 가짜 얼굴 검출 박스나 매칭 로직은 만들지 않는다. Figma 예시 데이터가 REQ와 다른 2곳은 REQ를 따른다: 실패 행에 이름을 붙이지 않음(REQ-FACE-005/007), 최근 인식 성공/실패 텍스트를 색상으로 구분함(REQ-FACE-007, Figma는 둘 다 회색). 전체화면은 브라우저 Fullscreen API로 같은 `<video>` 엘리먼트를 그대로 유지해 세션이 끊기지 않게 한다(REQ-FACE-007). Playwright의 `--use-fake-device-for-media-stream`으로 실제 카메라 스트림·전체화면 전환을 검증했다. |
| DEC-019 | 사용자 결정 · 웹 | 얼굴 인식 화면의 카메라 패널·최근 인식 목록 폭은 반응형으로 되돌렸다(카메라 `flex-1`로 뷰포트에 맞춰 늘어나고, 최근 인식 목록만 Figma 리터럴 626px 고정). 중간에 두 폭을 Figma 리터럴 값(910px/626px)으로 완전히 고정해봤으나 헤더의 목적 탭이 `w-full` 정렬 기준이라 아래 고정폭 콘텐츠와 어긋나는 새 버그가 생겼고, 그걸 고쳐 헤더까지 고정폭으로 맞춘 뒤에도 최종적으로 사용자가 반응형을 선택했다. 완전 고정폭은 1920px보다 좁은 화면에서 잘리는 문제가 있었던 반면 반응형은 그 문제가 없다. (당시엔 관리자-컴퓨터를 데스크톱 전용으로 알고 있었으나, DEC-020에서 정정 — 관리자도 폭 반응형이다.) "Figma 고정 픽셀을 기본값으로" 원칙은 화면별로 다시 판단한다 — 일괄 기본값으로 취급하지 않는다. |
| DEC-020 | 사용자 결정 · 웹 | 관리자도 학생과 같은 원칙으로 기기별 라우트를 나누지 않고 한 페이지를 반응형으로 처리한다(REQ-UI-005 갱신) — DEC-019 시점에 "관리자-컴퓨터는 데스크톱 전용"이라 판단한 것은 정정 대상이다. Tailwind 기본 breakpoint(`md`=768px, `xl`=1280px)가 사용자가 준 경계값(768/1280)과 정확히 일치해 커스텀 breakpoint 설정 없이 바로 사용했다. 레일 폭은 Figma 관리자-패드(노드 783:1221) 확인 결과 96px로 확정됐다(당초 88px는 임시값이었다). 모바일 로그아웃 우상단 고정 아이콘은 관리자-핸드폰 확인 결과 DEC-026으로 대체됐다. 얼굴 인식 화면은 `md` 미만에서 카메라·목록을 세로로 쌓는다(가로 배치 시 카메라가 과도하게 좁아짐) — 다른 다단 레이아웃 화면도 같은 규칙을 기본으로 삼는다. |
| DEC-021 | 사용자 결정 · 웹 | 관리자 홈 호실 그리드는 데스크톱(`xl`, 항상 7열×3행)에서 뷰포트 세로 크기와 무관하게 스크롤 없이 항상 화면 안에 맞도록 한다 — 넓은 화면일수록 `aspect-ratio` 카드가 커지면서 오히려 세로 스크롤이 심해지는 역설을 사용자가 지적("반응형이면 화면에 맞게 줄어드는 거 아니냐"). `xl`에서는 카드의 Figma 정사각형 비율(207.43:210)을 포기하고 그리드 셀 크기(`grid-rows-3` + 컨테이너 `flex-1`)에 맞춰 늘어나게 한다. 3행보다 많아지는 패드/폰(4열 이하)은 이 규칙에서 제외하고 원래대로 세로 스크롤한다 — 행이 많아질수록 화면 안에 다 우겨넣으면 카드가 읽을 수 없이 작아지기 때문이다. 패널 하단 여백도 처음엔 고정 `pb-[62px]`로 뒀다가, 화면이 커져도 여백은 안 늘어나는 문제를 사용자가 지적해 카드 영역과 여백을 Figma 1920 기준 비율(650:62)로 `flex-[650]`/`flex-[62]` 형제로 나눠 함께 스케일되게 정정했다(1920×1080에서 정확히 62px로 검증). 다른 관리자-컴퓨터 그리드 화면(봉사자 관리 등)도 행 수가 화면 크기와 무관하게 고정이라면 같은 패턴(칸+여백 모두 비례 스케일)을 기본으로 적용한다. |
| DEC-022 | 기본값 · 웹 | 봉사자 관리(REQ-COM-001/002)는 학생 전체 명단이 아직 없어(DEC-002 미확정) `mock-volunteers.ts`의 고정 10명으로 개발한다 — 명단 소속 여부(`isMember`)와 누적 횟수(`count`)를 분리해 제외 후 재추가해도 누적이 초기화되지 않음을 화면에서 보여준다(DEC-010). 추가 화면(`/admin/volunteers/add`)에 별도 "뒤로" 버튼을 넣지 않았다 — Figma(513:8)에 없고, 사이드바 활성 상태 판정(`isAdminNavActive`)이 이미 `startsWith`로 `/admin/volunteers/add`를 `/admin/volunteers`의 하위 경로로 인식해 "봉사자 관리" 네비게이션 항목이 두 화면 모두에서 활성 표시되므로 사이드바(데스크톱)·하단 탭바(폰)로 돌아갈 수 있다. |
| DEC-023 | 사용자 결정 · 웹 | 사용자가 "디자인 바뀌었는데 그거에 맞게"로 Figma 재대조를 요청해 확인: 봉사자 관리 화면(317:473)에 행별 "−"(1회 차감) 버튼이 추가됐다(0회에서는 테두리 `#ebebed`/글자 `#d5d5d8`의 비활성 상태, "봉사 1회 차감 (비활성)" 인스턴스). 확인 단계 없이 즉시 1회 차감하며, 0회 미만으로 내려가지 않는다(REQ-COM-002 갱신) — 차감 전용 성공/실패 토스트 문구는 Figma state messages(345:66/519:814)에 없어 임의로 만들지 않고 적립(+1) 토스트만 유지한다. 같은 갱신에서 "+ 봉사자 추가" 버튼 라벨이 "명단 편집"으로, 07번 화면 이름이 "봉사자 추가"에서 "봉사자 명단 편집"으로 바뀌었다(구조·검색/추가/제외 로직은 동일) — 화면 제목·버튼 텍스트만 변경, REQ 영향 없음. |
| DEC-024 | 사용자 결정 · 웹 | 사용자가 "우리 프로젝트는 PWA로 할거임"이라고 결정. 범위를 "설치 가능한 앱 셸만"으로 명시적으로 좁혔다(AskUserQuestion으로 확인) — manifest + 아이콘 + 홈 화면 추가만 지원하고, 서비스워커는 `/_next/static/`·`/icons/` 같은 정적 자산만 캐시-우선으로 캐싱하며 그 외 모든 요청(페이지 네비게이션·향후 API 포함)은 캐시를 타지 않고 네트워크로 직행한다. 웹 오프라인 얼굴 인식(DEC-005)과는 명시적으로 분리된 별개 결정이며, 이 PWA 범위가 DEC-005의 "오프라인 인식 가능"을 뒷받침하지 않는다. `app/manifest.ts`(Next 16 네이티브 Metadata API)와 손으로 쓴 `public/sw.js`를 썼다 — `next-pwa` 같은 webpack 플러그인은 Turbopack(DEC-015에서 빌드/개발 기본값으로 확정) 아래에서 `webpack()` 설정 훅이 실행되지 않아 서비스워커가 조용히 생성되지 않을 위험이 있어 배제했다. 앱 아이콘은 실제 로고가 없어 브랜드 라임(`#b9ee84`)/다크(`#17200a`) 색으로 만든 체크마크 placeholder다 — 실제 로고가 생기면 `public/icons/*.png`·`app/icon.png`를 교체한다. 서비스워커 등록은 production 빌드에서만 한다(2026-09-24): 개발 서버에서는 캐시 우선 정적 자산 캐시가 옛 CSS/JS를 계속 내줘 코드를 고쳐도 화면이 안 바뀌는 문제가 실제로 발생해(사용자 브라우저에서 확인) 등록을 막고 남아 있는 등록·`app-shell-*` 캐시는 해제·삭제한다. 하이드레이션이 load 이벤트보다 늦어 등록이 누락될 수 있어 이미 로드된 경우 즉시 등록한다. |
| DEC-025 | 사용자 결정 · 웹 | 사용자가 "관리자랑 사용자가 따로 있는데 스타트 url은 뭘로 했어"라고 물어 확인: 관리자/학생을 역할별로 따로 설치되게 한다(AskUserQuestion, "역할별로 따로 설치" 선택) — 관리자는 `/admin`으로 바로 열리는 별도 아이콘, 학생(아직 미구현)은 루트 manifest가 그 역할을 맡는다. Next.js의 `manifest.ts` 파일 컨벤션은 앱 루트에서만 라우트로 잡히고 하위 세그먼트(`app/admin/manifest.ts`)에서는 404가 나는 것을 빌드 후 직접 요청해 확인했다(icon.tsx류와 달리 세그먼트 스코프를 지원하지 않음) — 대신 `app/admin/manifest.webmanifest/route.ts` route handler로 직접 만들고 `app/admin/layout.tsx`의 `metadata.manifest` 필드로 연결했다. 두 manifest는 아이콘 이미지(placeholder, DEC-024)는 같고 `name`/`short_name`/`start_url`/`scope`만 다르다. `/admin` 하위 페이지 전부가 관리자 manifest를 상속하는지, iOS 홈 화면 타이틀("관리자")까지 갈리는지 실제 빌드로 확인했다. 학생 영역이 생기면 그 세그먼트에도 같은 route handler 패턴을 적용한다. |
| DEC-026 | 사용자 결정 · 웹 | 사용자가 Figma `관리자-핸드폰`(278:5)을 주고 "핸드폰으로 들어가면 관리자-핸드폰 디자인처럼 보이게" 반응형 구현을 요청. 디자인 재확인 중 조회 시점 사이에 디자이너가 모든 폰 화면 하단 탭바에 5번째 "로그아웃 탭"(icon/logout 18px, 빨간 아이콘)을 추가한 것을 노드 비교로 확인했다(탭 4개→5개, 폭 97.5→78px, 나머지 노드 변경 없음). 5탭·로그아웃 맨 오른쪽은 DEC-014(사용자 승인)와 같은 결정이며, 이 결정은 Figma 근거로 **라벨 없이 아이콘만 표시**함을 추가로 확정하고 DEC-012(폰 로그아웃 위치 위임)와 DEC-020의 우상단 고정 아이콘 임시안을 대체한다. 구현은 별도 폰용 컴포넌트를 만들지 않고 기존 컴포넌트에 폰 스타일을 기본값으로 두고 `md:`(768px)·`xl:`(1280px)에서 현재 스타일로 덮어쓰는 방식이다(카메라 스트림·QR 세션 중복 방지). Figma 프레임의 노치·상태바(9:41)는 기기 목업이라 구현하지 않는다. 폰의 로딩·빈 상태·오류 프레임은 봉사자 화면·다이얼로그(5·6번) 구현 이후로 미뤘다가(사용자 결정 2026-09-24) 얼굴 인식 화면에 구현했다: `RecentRecognitionsList`가 `status`(ready/loading/error)를 받고, 기록이 비면 빈 상태를 자동 표시하며, 빈·오류에서는 폰에서 카메라 패널을 숨기고 최근 인식 패널이 전체 높이를 쓴다. 실제 조회 API가 없어 loading/error는 화면 prop으로만 열어 두고 연결은 하지 않았다(기본 ready). 폰 state messages 프레임의 토스트 문구도 같은 방식으로 반영했다: 공용 `Toast`(`useToast`/`ToastLayer`, 기존 `StatusBanner`가 Figma 값과 동일)로 호실 출석 저장 성공·"변경된 내용이 없습니다."와 "이미 명단에 등록된 학생입니다."를 화면에서 바로 띄우고, 서버가 있어야 발생하는 항목은 표시 자리만 열었다(봉사자 관리 `listLoadFailed`, QR `sessionError` failed/expired, 얼굴 인식 `notice`). 적립·추가·제외·저장 실패 토스트와 3회 연속 인식 실패의 실제 발생은 API·인식 로직 연결 뒤로 남긴다. 별도 디자인이 있는 것은 얼굴 인식 화면의 로딩(최근 인식 스켈레톤)·빈 상태("최근 인식 기록이 없어요")·오류("불러오지 못했어요"+다시 시도)이고, QR 로딩/빈/오류 프레임은 정상 화면과 같아 별도 디자인이 없다. 관리자-패드는 링크 미수령으로 미확정. |
| DEC-027 | 사용자 결정 · 웹 | 학생 웹 QR 카메라(REQ-ATT-005)의 QR 판독은 `jsqr` 1.4.0(Apache-2.0, 의존성 없음, 타입 포함)을 쓴다 — 카메라 `<video>` 프레임을 canvas로 읽어 `jsQR(imageData, w, h)`에 넘긴다. 브라우저 기본 `BarcodeDetector`는 iOS Safari·Firefox에서 지원되지 않아 학생 휴대폰 대부분에서 동작하지 않고, `@zxing/browser`는 여러 바코드 형식을 다뤄 QR만 읽는 용도에 비해 크다. 프론트는 읽은 문자열을 그대로 서버(`POST /api/v1/qr/attendance`)에 넘기기만 하고, 토큰 유효성·만료·중복·운영일·현재 사용자 판정은 모두 서버가 한다(REQ-ATT-004/005). QR에 담을 내용 형식(휴대폰 기본 카메라로도 열리는 링크 + 서명/추측 불가 토큰)은 QR 백엔드 담당이 contracts에 정한다. 설치 시점 `npm audit` 0건. 외부 QR 판독 API로 영상·토큰을 보내지 않는다(DEC-017과 같은 이유). |

## 하네스 선택

- 공유 진입은 AGENTS.md, Claude는 `@AGENTS.md`로 가져온다.
- 스킬 원본은 `.agents/skills`, Claude 검색용은 기계적으로 생성하고 검사한다. Windows 링크 권한 불필요.
- 임의로 모델·권한 우회·전역 설정·외부 MCP를 지정하지 않는다.
- 현재 제공되는 것은 표준 Node 검사와 GitHub Actions 하네스 CI다. 이는 현재 구현 상태이며 CI 선구축을 제한하는 정책이 아니다. 추가 CI/CD 구성과 실행 기준은 DEC-013 및 [검증 기준](verification.md)을 따른다.
- 자동 Stop 훅은 넣지 않는다. 모델별 훅 지원 차이 없이 같은 로컬 명령과 CI로 검증한다.

근거와 문서 링크는 [출처 지도](sources/README.md)의 기술 자료에 있다.
