// 학생 웹 내부 알림 목록 — docs/spec/community.md REQ-COM-005 (공지 목록은 /notice)
import { StudentNotifications } from "@/components/student/StudentNotifications";

export default function Page() {
  return <StudentNotifications />;
}
